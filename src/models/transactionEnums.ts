export enum TransactionCategory {
    SALE = 'Venda',
    FOOD = 'Alimentação',
    HOME = 'Casa',
    WORK = 'Trabalho',
}

export enum TransactionType {
    ENTRY = 'Entrada',
    OUTCOME = 'Saída',
    TOTAL = 'Total',
}